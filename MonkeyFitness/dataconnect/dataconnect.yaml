specVersion: "v1"
serviceId: "monkeyfitness"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "monkeyfitness-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
